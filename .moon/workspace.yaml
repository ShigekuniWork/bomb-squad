projects:
  root: '.'
  server: './apps/server'

vcs:
  defaultBranch: 'main'
  hookFormat: 'bash'
  hooks:
    pre-commit:
      - 'moon run root:betterleaks-staged root:golang-lint root:golang-audit --affected --status=staged'
    commit-msg:
      - 'moon run root:commit-message'
  sync: true
